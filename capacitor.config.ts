import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.thesisync',
  appName: 'ThesiSync',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
